import { hospitalApi } from "../hospitalApi";

export default function HospitalChrome({ children, title, go, onAdd, active = "home" }) {
  const nav = [["⌂", "Home", "home"], ["♙", "Patient", "patients"], ["▣", "Appointment", "appointments"], ["▤", "Referrals", "referrals"], ["▥", "Report & Analysis", "analysis"], ["♟", "Staff Management", "staff-management"], ["◷", "Activity Log", "activity-log"], ["▰", "Message", "messages"], ["♟", "Notification", "notifications"]];
  const facilityName = hospitalApi.session()?.membership?.facility?.name?.trim() || "Facility";
  return <div className="hospital-app"><aside className="hospital-sidebar"><div className="hospital-logo">axon<span>l</span>ink</div><nav>{nav.map(([icon, label, route]) => <button className={active === route ? "active" : ""} onClick={() => go(route)} key={label}><i>{icon}</i>{label}</button>)}</nav><div className="hospital-account"><button className="hospital-setting-link" onClick={() => go("settings")}>⚙ &nbsp; Setting</button><span><i>✚</i><small><strong title={facilityName}>{facilityName}</strong>Facility account</small></span></div></aside><main className="hospital-main"><header className="hospital-topbar"><h1>{title}</h1><div className="hospital-user"><span className="hospital-mark">✚</span><span><b title={facilityName}>{facilityName}</b><small>Welcome back</small></span>{onAdd && <button className="quick-add" onClick={onAdd}>＋ Add appointment</button>}<button className="top-icon" aria-label="Open calendar" onClick={() => go("calendar")}>▣</button><button className="top-icon notification-bell" aria-label="Notifications">♟<em>0</em></button></div></header>{children}</main></div>;
}
